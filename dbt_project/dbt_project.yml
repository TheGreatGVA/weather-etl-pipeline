name: weather_pipeline
version: '1.0.0'
profile: weather_pipeline

model-paths: ["models"]
test-paths: ["tests"]

models:
  weather_pipeline:
    staging:
      +materialized: view
    analytics:
      +materialized: table
